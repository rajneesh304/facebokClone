apiVersion: v1
kind: Pod
metadata:
  name: client-pod
  labels:
    component: web
spec:
  containers:
    - name: client
      image: rajneesh304/facebook-clone
      ports:
        - containerPort: 3000
